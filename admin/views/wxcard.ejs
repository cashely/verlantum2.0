<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>代金券领取-云量检测</title>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,maximum-scale=1.0,minimum-scale=1.0,user-scalable=no">
    <script src="https://cdn.bootcss.com/jquery/3.5.0/jquery.min.js"></script>
  </head>
  <body>
    <div>

    </div>
    <script>

      if(!<%= broken %>) {
        const err = {
          code: '<%= err_code%>',
          msg:'<%= err_code_des%>'
        }
        if (err.code) {
          alert(err.msg)
        } else {
          alert('优惠券领取成功，下单后可选择使用')
        }
      }
      location.href="<%= url%>"
    </script>
  </body>
</html>
